# Baseline và Metric của bài toán

## Nghiên cứu thêm cách triển khai baseline cho bài toán LID

| Baseline | Input | Model | Fine-tune | Cách ánh xạ về VI/EN/OTHER |
| --- | --- | --- | --- | --- |
| B1. Whisper built-in LID | Log-Mel spectrogram 30s (hoặc audio ngắn hơn, pad/trim) | Whisper (tiny/base/small), qua hàm `detect_language` | Không | Lấy xác suất token ngôn ngữ 'vi' và 'en'; nếu ngôn ngữ có xác suất cao nhất không phải vi/en → OTHER |
| B2. ECAPA-TDNN (VoxLingua107) zero-shot | Waveform 16kHz mono | `speechbrain/lang-id-voxlingua107-ecapa`, phân loại 107 lớp | Không | Gộp xác suất các nhãn `vi: Vietnamese`, `en: English`; nếu nhãn top-1 không thuộc hai nhãn này hoặc tổng xác suất vi+en dưới ngưỡng → OTHER |
| B3. ECAPA frozen embedding + classifier tuyến tính | Waveform 16kHz mono | ECAPA-TDNN (đóng băng) trích embedding 256 chiều, sau đó Logistic Regression hoặc MLP nhỏ 3 lớp | Chỉ fine-tune classifier phía sau, backbone đóng băng | Classifier xuất trực tiếp 3 lớp VI/EN/OTHER, không cần quy tắc ánh xạ thủ công |
| B4 (tùy chọn). XLS-R zero-shot hoặc fine-tuned | Waveform 16kHz mono | XLS-R (300M–2B tham số), dùng làm feature extractor hoặc fine-tune đầu phân loại | Zero-shot: không; hoặc fine-tune nhẹ đầu phân loại | Tương tự B3, đầu phân loại xuất trực tiếp 3 lớp; nếu zero-shot, dùng embedding + classifier tuyến tính như B3 |

=> Về dataset, cả bốn baseline nên dùng cùng một cách chia dữ liệu để so sánh công bằng: tập FLEURS vi_vn và en_us cho hai lớp VI/EN, tập FLEURS zh_cn, ja_jp, ko_kr, fr_fr, th_th gộp lại cho lớp OTHER. B1 và B2 không cần tập train vì dùng mô hình pretrained nguyên bản, chỉ cần tập validation để tune threshold và tập test để đánh giá. B3 và B4 cần thêm một tập train nhỏ (embedding trích sẵn, huấn luyện classifier rất nhanh) trên FLEURS train split, validate trên FLEURS dev, rồi test trên FLEURS test cùng với VIVOS và LibriSpeech test-clean làm external test.

=> Baseline B2, ECAPA-TDNN pretrained trên VoxLingua107 dùng zero-shot với quy tắc ánh xạ nhãn, nên được triển khai đầu tiên. Lý do là mô hình không đòi hỏi huấn luyện, có độ chính xác gốc cao trên tập 107 ngôn ngữ bao gồm cả tiếng Việt, kích thước nhỏ nên triển khai và đo latency nhanh, đồng thời sẵn embedding để có thể nâng cấp lên B3 gần như ngay lập tức khi cần. 

**Thứ tự triển khai cơ bản**

Quy trình thực nghiệm nên đi theo năm bước liên tiếp. 
- B1: chạy zero-shot B1 và B2 trên cùng tập test FLEURS đã chia cân bằng, thu Accuracy, Macro-F1 và per-class F1 để có mức tham chiếu ban đầu mà không cần huấn luyện gì. 
- B2: nếu B2 cho Macro-F1 chưa đạt mục tiêu, huấn luyện classifier B3 trên embedding đã trích, so sánh trực tiếp với B2 trên cùng tập test. 
- B3: đánh giá cả ba baseline trên external test VIVOS và LibriSpeech để kiểm tra generalization ngoài phân bố FLEURS. 
- B4: tune threshold quyết định UNKNOWN trên tập validation riêng, không đụng vào test set, rồi đo lại toàn bộ metric bao gồm cả nhánh reject. 
- B5: chỉ sau khi có một baseline ổn định mới mở rộng sang sliding-window LID và code-switch detection, tránh việc gộp hai bài toán khó cùng lúc ngay từ đầu.

**Thứ tự triển khai xử lý code switching**

=> Sau khi baseline VI/EN/OTHER ổn định, và nên tái sử dụng chính backbone LID đã có thay vì huấn luyện mô hình mới từ đầu.
- B1: chọn kích thước cửa sổ trượt, thường 500ms-1s với overlap khoảng 50%; cửa sổ quá ngắn khiến LID không đủ tín hiệu âm học để tự tin, quá dài thì mất khả năng bắt đúng điểm chuyển ngôn ngữ, nên cần thử vài kích thước và chọn theo Segment-level Accuracy. 
- B2: chạy lại chính backbone đã chọn ở Phần 2 (B2 hoặc B3) trên từng cửa sổ thay vì trên cả câu, dùng lại embedding và classifier đã có. 
- B3: thêm temporal smoothing hoặc hysteresis để làm mượt chuỗi nhãn theo cửa sổ, ví dụ majority voting trên k cửa sổ liên tiếp hoặc chỉ công nhận chuyển ngôn ngữ khi N cửa sổ liên tiếp cùng đổi nhãn, tránh việc một từ mượn ngắn kích hoạt chuyển nhãn giả. 
- B4: gộp chuỗi nhãn theo thời gian đã làm mượt thành một nhãn câu VI, EN hoặc MIXED, đồng thời xác định matrix language (ngôn ngữ chiếm đa số thời lượng) để phục vụ Language State Manager quyết định response_language.

## Nghiên cứu các metric đánh giá để làm nổi bật lên sự vượt trội của hệ thống khi tích hợp thêm mô hình LID

**3.1. Metric cho Language Identification**

* **Accuracy:** Tỷ lệ dự đoán đúng trên toàn bộ tập test. Dễ hiểu nhưng có thể gây sai lệch nếu dữ liệu mất cân bằng giữa VI, EN và OTHER.
* **Precision / Recall theo từng lớp:** Cho biết mô hình dự đoán từng ngôn ngữ tốt đến đâu. Đặc biệt cần chú ý **Recall của OTHER** để tránh cho ngôn ngữ không hỗ trợ đi tiếp vào ASR/LLM.
* **Macro-F1:** Tính F1 trung bình của các lớp với trọng số bằng nhau, phù hợp khi dữ liệu giữa VI, EN và OTHER không cân bằng.
* **Balanced Accuracy:** Trung bình Recall của các lớp, giúp đánh giá khả năng nhận diện đồng đều giữa các ngôn ngữ.
* **Confusion Matrix:** Cho biết mô hình thường nhầm lớp nào với lớp nào, ví dụ VI → EN hay OTHER → VI.

**Metric chính nên dùng: Macro-F1**, vì phản ánh công bằng hiệu năng trên cả ba lớp VI, EN và OTHER.

**3.2. Metric cho UNKNOWN và cơ chế reject**

* **Unknown Precision:** Trong các mẫu bị từ chối, bao nhiêu mẫu thực sự nên bị từ chối.
* **Unknown Recall:** Trong các mẫu thực sự không chắc chắn, bao nhiêu mẫu được phát hiện đúng là UNKNOWN.
* **Unknown F1:** Tổng hợp Precision và Recall của UNKNOWN.
* **False Acceptance Rate (FAR):** Tỷ lệ mẫu đáng lẽ phải UNKNOWN nhưng lại bị nhận thành VI/EN. Đây là lỗi cần hạn chế nhất.
* **False Rejection Rate (FRR):** Tỷ lệ mẫu hợp lệ nhưng bị từ chối nhầm.
* **Coverage:** Tỷ lệ mẫu mà hệ thống chấp nhận đưa ra dự đoán thay vì UNKNOWN.
* **Selective Accuracy:** Độ chính xác chỉ trên những mẫu được hệ thống chấp nhận.
* **AUROC/AUPRC:** Đánh giá khả năng dùng confidence để phân biệt mẫu đáng tin và không đáng tin.

Threshold cho UNKNOWN nên được chọn trên **validation set**, sau đó cố định và đánh giá duy nhất một lần trên test set.

**3.3. Metric cho code-switching**

* **Code-switch Precision / Recall / F1:** Đánh giá khả năng phân loại câu thành VI, EN hoặc MIXED.
* **Switch-point F1:** Đánh giá khả năng phát hiện đúng vị trí chuyển ngôn ngữ.
* **Boundary Error:** Đo độ lệch thời gian giữa điểm chuyển ngôn ngữ dự đoán và thực tế.
* **Segment-level LID Accuracy:** Độ chính xác nhận diện ngôn ngữ trên từng đoạn nhỏ của audio.
* **Frame/Window-level Accuracy:** Độ chính xác trên từng cửa sổ thời gian ngắn.

Trong giai đoạn đầu, **F1 cho MIXED, Switch-point F1 và Segment-level Accuracy** là đủ để đánh giá hệ thống.

**3.4. Metric cho ASR**

* **WER:** Tỷ lệ lỗi ở mức từ.
* **CER:** Tỷ lệ lỗi ở mức ký tự, thường hữu ích với tiếng Việt.
* **Code-switch WER / Mixed Error Rate:** Đánh giá riêng lỗi ASR trên các câu có trộn VI–EN.
* **English-word Error Rate:** Đánh giá riêng các từ tiếng Anh xuất hiện trong câu tiếng Việt.

Nên báo cáo WER/CER riêng cho **VI-only, EN-only và MIXED**, thay vì chỉ dùng một kết quả tổng.

**3.5. Metric end-to-end**

* **Response Language Accuracy:** Tỷ lệ hệ thống trả lời đúng ngôn ngữ của người dùng.
* **Task Success Rate:** Tỷ lệ hội thoại hoàn thành đúng mục tiêu.
* **End-to-end Latency:** Tổng thời gian từ lúc nhận audio đến lúc trả lời.
* **Real-time Factor (RTF):** So sánh thời gian xử lý với độ dài audio.
* **API Call Reduction:** Tỷ lệ lượt gọi ASR/LLM được tránh nhờ phát hiện OTHER hoặc UNKNOWN sớm.
* **Wrong-language Response Rate:** Tỷ lệ hệ thống trả lời sai ngôn ngữ.

=> Các metric này giúp chứng minh rằng việc thêm LID không chỉ cải thiện khả năng nhận diện ngôn ngữ mà còn giúp **giảm lỗi ASR, giảm phản hồi sai ngôn ngữ, tiết kiệm API call và cải thiện chất lượng toàn bộ Voice Agent**.
