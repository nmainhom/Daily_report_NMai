# 4 MODEL CẦN TRIỂN KHAI

## 1. Audio Flamingo Next (AF-Next), NVIDIA và ĐH Maryland

**Là gì.** Đây là một Large Audio-Language Model (LALM) mã nguồn mở. Nó ghép audio encoder với một LLM decoder-only để hỏi đáp, captioning, phiên âm và suy luận trực tiếp trên audio, tương tự LLaVA nhưng cho âm thanh (speech, âm thanh môi trường và nhạc trong cùng một model). Có ba biến thể: Instruct cho hỏi đáp chung, Think cho suy luận nhiều bước, Captioner cho mô tả chi tiết. Model nhận audio dài tới 30 phút và được huấn luyện trên hơn 1 triệu giờ audio.

**Dùng để làm gì.** ASR, hiểu paralinguistic, dịch giọng nói đa ngôn ngữ, hỏi đáp về âm thanh và mô tả nhạc. Điểm mạnh là audio dài như podcast, video dài, hội thoại nhiều người.

**Hoạt động ra sao.**
- Audio được đưa về 16 kHz mono, chuyển thành log-mel 128 kênh rồi qua AF-Whisper (encoder dựa trên Whisper) theo từng chunk 30 giây. Một adaptor MLP 2 lớp đưa đặc trưng vào Qwen-2.5-7B, context được mở rộng từ 32k lên 128k token.
- Rotary Time Embeddings (RoTE) mã hóa vị trí token theo timestamp thực của audio thay vì thứ tự chuỗi, giúp suy luận về thời gian.
- Temporal Audio Chain-of-Thought yêu cầu mỗi bước lập luận gắn với một mốc thời gian trong audio. Cách này giảm hallucination trên bản ghi dài.
- Huấn luyện gồm 4 giai đoạn: pre-training (chỉ adaptor rồi thêm encoder), mid-training (fine-tune toàn bộ, mở rộng lên 30 phút/128K, cho ra bản Captioner), post-training bằng GRPO (ra bản Instruct), rồi CoT-training (ra bản Think).

**Kết quả và lưu ý.** Trên LongAudioBench, AF-Next-Instruct đạt 73.9, cao hơn Gemini 2.5 Pro (60.4). WER trên LibriSpeech là 1.54 (clean) và 2.76 (other). Checkpoint trên Hugging Face là model audio+text → text. Phần streaming TTS và voice-to-voice thuộc dự án nhưng không nằm trong checkpoint này. License là NVIDIA OneWay Noncommercial, tức phi thương mại.

## 2. Step-Audio-R1.5, StepFun

**Là gì.** Model suy luận audio (32B) nối tiếp Step-Audio-R1. Ý tưởng chính: các audio reasoning model trước đó dùng RL với phần thưởng kiểm chứng được (RLVR), nên điểm benchmark cao nhưng hội thoại trở nên cụt lủn, máy móc, mất prosody và cảm xúc trong hội thoại dài. Nhóm gọi đó là "verifiable reward trap" và chuyển sang RLHF.

**Dùng để làm gì.** Hội thoại giọng nói nhiều lượt cần giữ ngữ cảnh và tự nhiên, cùng nhận biết đặc điểm giọng (tuổi, giới tính, tốc độ, cảm xúc, accent). Các benchmark mới của họ như Step-Caption đo đúng nhóm năng lực này.

**Hoạt động ra sao.**
- Kiến trúc có ba phần. Encoder Qwen2 (25 Hz, đóng băng) đưa qua adaptor giảm tần số xuống 12.5 Hz, rồi tới LLM Qwen2.5-32B. LLM sinh chuỗi suy luận trước rồi mới ra câu trả lời, và đầu ra là văn bản thuần.
- Huấn luyện gồm mid-training tập trung audio, cold-start SFT trên hội thoại nhiều lượt (giữ ngữ cảnh, làm theo chỉ dẫn, tự nhiên), rồi RLHF.
- RLHF dùng generated reward model so sánh câu trả lời của policy với câu tham chiếu. Nếu mẫu có tiêu chí rõ thì reward model chấm theo rubric, nếu không thì so sánh cặp thông thường. Phần tối ưu theo kiểu PPO.

**Kết quả và lưu ý.** Điểm trung bình 77.97, so với 72.50 của R1 và 79.67 của Gemini 3 Pro. Trên Audio MultiChallenge, R1.5 đạt 41.15 so với 24.61 của R1. Nhóm tự chạy các baseline qua API bằng khung đánh giá riêng và dùng cả benchmark do họ tạo, nên nên xem đây là số tự báo cáo. Repo đã công bố báo cáo R1.5 và ba benchmark nội bộ, còn trọng số được nhắc rõ là của R1 và R1.1. Mình chưa thấy nguồn nào nói R1.5 đã có trọng số công khai.

## 3. Qwen3.5-Omni, Alibaba Qwen

**Là gì.** LLM omnimodal đầy đủ: hiểu văn bản, ảnh, audio và video có tiếng, đồng thời sinh văn bản hoặc giọng nói theo thời gian thực. Nó được pretrain native trên hơn 100 triệu giờ dữ liệu audio-visual, có hai bản Plus và Flash với context 256k, và truy cập qua API. Quy mô lên tới hàng trăm tỷ tham số. Mình không thấy nói về việc mở trọng số.

**Dùng để làm gì.** Ba nhóm năng lực mới: captioning audio-visual có kiểm soát (có timestamp, phân đoạn cảnh), tương tác thời gian thực (ngắt lời theo ngữ nghĩa, điều khiển âm lượng/tốc độ/cảm xúc bằng giọng nói, clone giọng từ mẫu), và hành vi agent (tự gọi WebSearch, function call, "Audio-Visual Vibe Coding" là sinh code từ chỉ dẫn bằng âm thanh/hình ảnh).

**Hoạt động ra sao.**
- Kiến trúc Thinker–Talker. Thinker nhận ảnh và audio qua vision encoder và AuT, rồi sinh văn bản. Talker nhận biểu diễn từ Thinker và sinh token giọng nói streaming. Cả hai dùng Hybrid-Attention MoE.
- AuT là audio encoder huấn luyện từ đầu trên 40 triệu giờ dữ liệu audio-văn bản, cho ra token ở 6.25 Hz.
- ARIA (Adaptive Rate Interleave Alignment) căn chỉnh động đơn vị văn bản và giọng nói khi giải mã streaming, giảm lỗi bỏ từ, phát âm sai và đọc số mơ hồ.
- Talker dự đoán codec nhiều codebook bằng module MTP, rồi Code2Wav dựng waveform từng khung. Độ trễ gói đầu tiên với đầu vào audio là 435 ms (Plus) và 235 ms (Flash).
- Giai đoạn post-training cuối là Interaction-Aligned RL. Giai đoạn này nhắm vào các lỗi hội thoại nhiều lượt như tự ý đổi ngôn ngữ (code-switching ngoài ý muốn), lệch persona và giảm khả năng làm theo chỉ dẫn khi ngữ cảnh dài.

**Ngôn ngữ.** Bảng ngôn ngữ trong báo cáo liệt kê tiếng Việt ở cả đầu vào giọng nói lẫn đầu ra giọng nói. Con số tổng số ngôn ngữ trong báo cáo không hoàn toàn nhất quán giữa phần abstract và phần thân, nên mình chỉ chắc về việc có tiếng Việt trong bảng.

## 4. AudioInteraction (Audio Interaction Model)

**Là gì.** Nhóm tác giả cho rằng các LALM hiện nay bị kẹt trong mô hình offline: đưa cả đoạn audio, chờ rồi mới nhận trả lời. Các model streaming thì mỗi cái chỉ làm một tác vụ. Họ đề xuất khái niệm "Audio Interaction Model" và xây model đầu tiên, luôn bật, chủ động, chạy được cả tác vụ offline (ASR, dịch, AQA) lẫn streaming (voice chat) trong một model duy nhất. Model 3B, dựa trên Qwen2.5-Omni.

**Dùng để làm gì.** Một model always-on thay cho nhiều model rời. Ví dụ trong README: khi báo cháy bắt đầu kêu lúc người dùng đang im lặng, model tự chuyển từ ⟨Silent⟩ sang ⟨Speak⟩ để cảnh báo mà không cần được hỏi. Nó cũng có thể vừa nghe nhạc nền vừa trò chuyện và dịch/phiên âm từng chunk.

**Hoạt động ra sao (SoundFlow).**
- Ở mỗi chunk audio, model chọn giữa ⟨Speak⟩ và ⟨Silent⟩, nên nhận dạng, dịch và hội thoại đều là chỉ dẫn trong cùng một vòng lặp perceive–decide–respond.
- Pipeline gồm ghép clip ngắn thành tương tác dài để tạo dữ liệu, huấn luyện quyết định theo chunk (có "history review" và "comprehension-aware silence"), và suy luận FIFO bất đồng bộ giúp giảm độ trễ khung đầu tiên 4.5 lần.
- Dữ liệu StreamAudio-2M có khoảng 2.6 triệu mẫu (7.4 triệu lượt, 66.7 nghìn giờ), gồm clip thật từ AudioSet, CommonVoice, CoVoST2 v.v., text được tổng hợp thành giọng bằng CosyVoice rồi ghép thành chuỗi streaming có noise môi trường.

## 5.MOSS-Audio

**Là gì** MOSS-Audio là model nguồn mở chuyên hiểu âm thanh: nghe một đoạn audio rồi trả lời hoặc mô tả bằng văn bản. Nó do MOSI.AI, nhóm OpenMOSS và Shanghai Innovation Institute phát triển. Model này không phải model hội thoại, không sinh giọng nói và không phải model LID.

**Dùng để làm gì** Nó gộp nhiều năng lực trong một model: hiểu lời nói, âm thanh môi trường, nhạc, tạo caption cho audio, hỏi đáp có nhận biết thời gian và suy luận phức tạp. Cụ thể: 
- Phiên âm có timestamp. Hỗ trợ căn thời gian ở mức từ và mức câu. 
- Phân tích người nói. Nhận diện đặc điểm người nói, phân tích trạng thái cảm xúc từ giọng điệu và ngữ cảnh, và phát hiện các sự kiện âm thanh chính. 
- Trích ngữ cảnh cảnh vật. Rút tín hiệu từ tiếng nền, noise môi trường, nhạc và âm không phải lời nói để suy ra bối cảnh. 
- Hỏi đáp và tóm tắt. Trả lời câu hỏi và tóm tắt podcast, cuộc họp, phỏng vấn, bản ghi môi trường. 
- Suy luận nhiều bước. Dựa trên huấn luyện chain-of-thought và reinforcement learning. 

**Hoạt động ra sao** Kiến trúc gồm ba phần: audio encoder, adapter và LLM. Audio được MOSS-Audio-Encoder biến thành biểu diễn liên tục ở 12.5 Hz, adapter chiếu vào không gian embedding của LLM, rồi LLM sinh văn bản. Nhóm tác giả tự huấn luyện encoder từ đầu thay vì dùng front-end có sẵn. Hai điểm thiết kế đáng chú ý: 
- DeepStack. Ngoài đầu ra tầng cuối, đặc trưng từ các tầng sớm và giữa của encoder cũng được chiếu riêng rồi đưa vào các tầng đầu của LLM, để giữ lại nhịp, âm sắc và các sự kiện thoáng qua. 
- Time marker. Trong pretraining, các token thời gian được chèn giữa các khung audio theo khoảng cố định, giúp model học "điều gì xảy ra lúc nào", từ đó làm được timestamp ASR, định vị sự kiện và QA theo thời gian. 
GitHub

