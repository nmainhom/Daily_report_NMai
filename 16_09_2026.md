# Quy trình chuẩn hóa dữ liệu âm thanh

## 1. Mục đích của chuẩn hóa dữ liệu âm thanh

Chuẩn hóa dữ liệu âm thanh là bước tiền xử lý nhằm đưa các file audio về cùng một định dạng và chất lượng tương đối đồng nhất trước khi sử dụng để huấn luyện mô hình. Dữ liệu âm thanh thu thập từ nhiều nguồn có thể khác nhau về sample rate, số kênh, âm lượng, thời lượng, mức nhiễu hoặc khoảng lặng. Nếu đưa trực tiếp các file không đồng nhất này vào mô hình, mô hình có thể phải học cả những khác biệt không liên quan đến nội dung lời nói, làm giảm hiệu quả huấn luyện. Vì vậy, chuẩn hóa giúp giảm sự khác biệt không cần thiết giữa các file và tạo đầu vào ổn định hơn cho các mô hình xử lý tiếng nói.

## 2. Kiểm tra dữ liệu trước khi chuẩn hóa

Trước khi thay đổi dữ liệu, cần kiểm tra các đặc trưng cơ bản của từng file audio như định dạng file, sample rate, số channel, thời lượng, giá trị peak, RMS và giá trị trung bình của waveform. Ngoài ra cần kiểm tra xem file có bị hỏng, có thời lượng bằng 0, chứa quá nhiều khoảng lặng hoặc bị clipping hay không. Việc kiểm tra trước giúp tránh xử lý dữ liệu một cách máy móc và xác định chính xác những bước nào thực sự cần thiết. Ví dụ, nếu toàn bộ dữ liệu đã có sample rate 16 kHz và mono thì không cần resample hoặc chuyển đổi channel thêm một lần nữa.

## 3. Chuẩn hóa sample rate

Sample rate thể hiện số mẫu âm thanh được ghi lại trong một giây. Các file trong cùng một bộ dữ liệu nên sử dụng cùng sample rate để mô hình nhận được đầu vào đồng nhất. Đối với nhiều bài toán speech như nhận dạng giọng nói, Wav2Vec2, HuBERT hoặc các mô hình speech representation, 16 kHz là một mức sample rate phổ biến. Vì vậy, nếu dữ liệu có các file 44.1 kHz, 48 kHz hoặc các sample rate khác nhau, chúng có thể được resample về 16 kHz. Tuy nhiên, nếu file đã ở 16 kHz thì không cần resample vì việc chuyển đổi không cần thiết có thể làm thay đổi tín hiệu gốc.

## 4. Chuẩn hóa số kênh âm thanh

Audio có thể được lưu dưới dạng mono hoặc stereo. Mono chỉ chứa một kênh âm thanh, trong khi stereo chứa hai kênh trái và phải. Với các bài toán nhận dạng nội dung lời nói, thông tin vị trí trái phải thường không quan trọng, do đó dữ liệu thường được chuyển về mono. Một cách đơn giản là lấy trung bình tín hiệu của hai kênh trái và phải. Việc chuyển stereo sang mono cũng giúp giảm lượng dữ liệu cần xử lý. Tuy nhiên, với các bài toán như xác định vị trí nguồn âm hoặc xử lý microphone array thì thông tin nhiều kênh cần được giữ lại.

## 5. Chuẩn hóa biên độ và âm lượng

Các file audio có thể có mức âm lượng rất khác nhau. Vì vậy cần kiểm tra các giá trị như peak và RMS để phát hiện những file quá nhỏ hoặc quá lớn. Tuy nhiên, không nên mặc định đưa peak của tất cả các file lên giá trị tối đa bằng 1 vì thao tác này có thể khuếch đại cả tiếng nhiễu. Thay vào đó, nên kiểm tra mức âm lượng tổng thể và chỉ áp dụng RMS normalization hoặc loudness normalization khi sự chênh lệch giữa các file thực sự lớn. Nếu mức RMS giữa các file đã tương đối đồng đều thì có thể giữ nguyên tín hiệu để hạn chế việc biến đổi dữ liệu gốc.

## 6. Kiểm tra DC offset và clipping

Waveform của tín hiệu âm thanh thông thường nên dao động quanh giá trị 0. Nếu giá trị trung bình của waveform lệch đáng kể khỏi 0, tín hiệu có thể tồn tại DC offset và có thể được hiệu chỉnh bằng cách trừ giá trị trung bình khỏi waveform. Bên cạnh đó, cần kiểm tra clipping, tức là tình trạng biên độ tín hiệu chạm hoặc vượt giới hạn tối đa trong thời gian đáng kể. Clipping khiến waveform bị méo và có thể làm giảm chất lượng dữ liệu. Những file clipping nhẹ có thể được giữ lại tùy bài toán, còn các file bị méo nghiêm trọng nên được loại bỏ hoặc xử lý riêng.

## 7. Xử lý khoảng lặng

Trong dữ liệu speech thường tồn tại khoảng lặng ở đầu, cuối hoặc giữa các câu. Các khoảng lặng rất dài ở đầu và cuối file có thể được cắt bớt để giảm dữ liệu không cần thiết. Tuy nhiên, không nên loại bỏ toàn bộ khoảng nghỉ giữa các từ và câu vì các khoảng nghỉ tự nhiên cũng là một phần của tín hiệu lời nói. Đặc biệt với dữ liệu code-switching, việc cắt quá mạnh có thể làm mất phần chuyển tiếp giữa hai ngôn ngữ. Do đó, nên giữ các khoảng nghỉ ngắn và chỉ xử lý những đoạn silence kéo dài.

## 8. Sử dụng Voice Activity Detection

Voice Activity Detection, hay VAD, được sử dụng để xác định những khoảng thời gian có tiếng nói của con người trong file audio. VAD giúp phân biệt tương đối giữa vùng speech và vùng không có speech, từ đó hỗ trợ loại bỏ silence dài và chia audio thành các đoạn nhỏ. So với việc chỉ sử dụng ngưỡng biên độ, VAD thường phù hợp hơn vì các đoạn nhiễu môi trường có thể có biên độ cao nhưng không chứa lời nói. Các công cụ như Silero VAD hoặc WebRTC VAD thường được sử dụng trong pipeline xử lý dữ liệu speech.

## 9. Chia audio thành các segment

Những file audio quá dài thường không phù hợp để đưa trực tiếp vào phần lớn các mô hình speech. Do đó, cần chia chúng thành các đoạn ngắn hơn. Việc chia đoạn nên dựa trên ranh giới lời nói hoặc khoảng nghỉ tự nhiên thay vì cắt cố định theo từng khoảng 10 giây, vì cách cắt cố định có thể chia đôi một từ hoặc một câu. Một khoảng thời lượng phổ biến có thể từ vài giây đến khoảng 20 giây tùy mô hình và bài toán. Các đoạn quá ngắn thường chứa quá ít thông tin, trong khi các đoạn quá dài làm tăng yêu cầu bộ nhớ khi huấn luyện.

## 10. Xử lý nhiễu

Không phải tất cả tiếng nhiễu đều cần được loại bỏ. Một lượng nhiễu môi trường nhẹ như tiếng quạt, tiếng phòng hoặc microphone noise có thể giúp mô hình thích nghi tốt hơn với dữ liệu thực tế. Chỉ nên loại bỏ hoặc denoise những file có mức nhiễu quá lớn khiến nội dung lời nói khó nhận biết. Nếu áp dụng denoise quá mạnh cho toàn bộ dữ liệu, tín hiệu giọng nói có thể bị biến dạng và dataset trở nên khác xa điều kiện thực tế.

## 11. Kiểm tra chất lượng sau khi chia đoạn

Sau khi thực hiện VAD và segmentation, cần kiểm tra lại từng segment về thời lượng, RMS, peak, clipping và mức năng lượng. Một file dài có thể có chất lượng tổng thể tốt nhưng vẫn chứa một vài đoạn gần như im lặng hoặc rất nhiễu. Vì vậy, quality filtering nên được thực hiện ở mức segment trước khi đưa dữ liệu vào huấn luyện. Các đoạn quá ngắn, chỉ chứa silence, bị lỗi hoặc gần như không nghe rõ speech nên được loại bỏ.

## 12. Tạo metadata cho dữ liệu

Sau quá trình chuẩn hóa nên tạo một file metadata để quản lý dataset. Metadata có thể chứa tên file, nguồn audio gốc, thời lượng, sample rate, RMS, vị trí bắt đầu và kết thúc của segment và transcript nếu có. Ví dụ, một segment có thể được lưu dưới tên `ch01_003.wav` kèm thông tin cho biết nó được cắt từ `ch01.wav`, bắt đầu ở giây 32.4 và kết thúc ở giây 41.7. Việc lưu metadata giúp dễ dàng kiểm tra, train, đánh giá và truy vết dữ liệu sau này.

## 13. Pipeline chuẩn hóa đề xuất

Một quy trình chuẩn hóa dữ liệu speech hoàn chỉnh có thể bắt đầu bằng việc kiểm tra file audio, sau đó thống nhất sample rate và số channel nếu cần. Tiếp theo là kiểm tra amplitude, DC offset, clipping và mức âm lượng. Sau khi đảm bảo chất lượng cơ bản, VAD được sử dụng để xác định vùng chứa speech, loại bỏ những khoảng lặng quá dài và chia audio thành các segment hợp lý. Cuối cùng, các segment được kiểm tra lại chất lượng, lưu vào thư mục processed và tạo metadata để phục vụ quá trình train.

Đối với bộ dữ liệu hiện tại gồm các file 16 kHz, mono và WAV, phần chuẩn hóa định dạng cơ bản gần như đã hoàn thành. Do đó, công việc tiếp theo nên tập trung vào **VAD, xử lý silence, segmentation, quality filtering và tạo metadata**, thay vì tiếp tục resample hoặc thay đổi amplitude không cần thiết.
