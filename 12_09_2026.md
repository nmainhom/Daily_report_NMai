**Nền tảng DSP**

- Số hóa tín hiệu đòi hỏi tần số lấy mẫu lớn hơn hai lần tần số cao nhất, nếu không sẽ xảy ra aliasing khiến tần số cao giả dạng tần số thấp.
- Hệ LTI được đặc trưng hoàn toàn bởi đáp ứng xung, với đầu ra là tích chập giữa tín hiệu vào và đáp ứng xung đó.
- FFT là công cụ phân tích tần số chủ lực, kèm một nguyên lý bất định không tránh được: cửa sổ ngắn cho phân giải thời gian tốt nhưng phân giải tần số kém, và ngược lại.
- Trong thiết kế bộ lọc, FIR luôn ổn định và pha tuyến tính nhưng tốn tính toán, còn IIR hiệu quả hơn nhưng đánh đổi bằng pha phi tuyến.

**Mel-spectrogram và MFCC**

- Tai người cảm nhận logarit cả cao độ lẫn cường độ, nên thang Mel phóng to vùng tần thấp và nén vùng tần cao.
- Tiếng nói tựa dừng trong khoảng 20–40 ms, đó là cơ sở chọn khung 25 ms cho STFT.
- Mel filterbank vừa giảm chiều từ khoảng một nghìn bin FFT xuống 40–128, vừa làm mượt phổ để giữ lại đường bao.
- MFCC đi thêm một bước DCT, dựa trên mô hình source–filter: lấy log biến tích chập thành phép cộng, tách được thành phần cao độ (biến thiên nhanh, cần bỏ) khỏi thành phần formant (biến thiên chậm, cần giữ).
- DCT chủ yếu tồn tại vì lý do lịch sử là khử tương quan giữa các dải Mel chồng lấn, cho phép GMM dùng hiệp phương sai đường chéo.
- Nguyên tắc chọn lựa: MFCC cho mô hình cổ điển và thiết bị nhúng, mel-spectrogram cho CNN và Transformer vì DCT phá vỡ tính cục bộ theo tần số.

**Mô hình ML và HMM**

- Lĩnh vực đi từ GMM-HMM thống trị 25 năm, sang DNN-HMM năm 2012, rồi CTC và các kiến trúc end-to-end như RNN-Transducer, Conformer, Whisper.
- HMM suy ra chuỗi trạng thái ẩn (âm vị) từ chuỗi quan sát (MFCC).
- Ba bài toán kinh điển: Forward tính xác suất, Viterbi giải mã chuỗi trạng thái, Baum-Welch học tham số.
- Cơ chế self-loop giúp HMM tự xử lý biến thiên tốc độ nói, ưu điểm khiến nó thắng DTW.
- Điểm yếu nền tảng là giả định độc lập đầu ra, vốn sai với tiếng nói.
- Ngày nay HMM vẫn sống mạnh qua forced alignment — bước bắt buộc khi chuẩn bị dữ liệu TTS.

